{
    "name": "pre-placement Q&A MERN app",
    "version": "1.0.0",
    "engines": {
      "node": "8.x.x",
      "npm": "5.x.x"
    },
    "private": true,
    "description": "Create PPQA React App with Express API and Postgres in Heroku",
    "scripts": {
      "postinstall":
        "npm --prefix client install && npm --prefix server install",
      "start:dev":
        "concurrently 'npm run --prefix client start' 'npm run --prefix server start'",
      "heroku-postbuild": "npm run --prefix client build",
      "start": "cd server && NODE_ENV=production node server.js"
    },
    "author": "Ari Autio <ari.autio@iki.fi>",
    "license": "MIT",
    "devDependencies": {
      "concurrently": "^3.5.0"
    },
    "cacheDirectories": [
      "node_modules",
      "client/node_modules",
      "server/node_modules"
    ]
  }